// Get data from sql table
// Create a function

// Given array of objects
// label (goForRun)
// Data (length)

// Export function

async function temp(habitId) {
  const response = await fetch("/");
}
